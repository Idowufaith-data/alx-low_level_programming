#include<stdio.h>

/**
 * main - entry point
 *
 * Puts()-Writes a string to the output
 *
 * Return: Always 0
 */
int main(void)
{
	char str[] = "Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
